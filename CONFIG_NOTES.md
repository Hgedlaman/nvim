For markdown viewer, the following need to be run in the terminal (**AFTER INSTALLING THE PLUGIN BY JUST RUNNING nvim**): 

```bash
cd ~/.local/share/nvim/site/pack/core/opt/markdown-preview.nvim/app
npx --yes yarn install
```

IF ON WSL, the following: 

```bash
sudo apt install xdg-utils
```
